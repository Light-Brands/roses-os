import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Enroll',
  description:
    'Begin your enrollment in International Aura School — review the sacred agreements and join a living path of Rose Meditation, Aura Reading, and coherence.',
};

export default function EnrollLayout({ children }: { children: React.ReactNode }) {
  return children;
}
